filebeat.inputs:
- type:  filestream
  paths:
    - /logs/*.json
  parsers:
    - ndjson:
      overwrite_keys: true
      expand_keys: true
      add_error_key: ture


output.logstash:
  hosts: ["logstash:5044"]